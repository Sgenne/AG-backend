/**
 * Indicates that the requested service could not be fulfilled due to a database error.
 */
export const DATABASE_ERROR = "database error";

/**
 * Indicates that a resource could not be created since a clashing resource already exists.
 */
export const RESOURCE_ALREADY_EXISTS = "resource already exists";

/**
 * Indicates that the requested service could not be fulfilled since a required
 * resource could not be found.
 */
export const RESOURCE_NOT_FOUND = "resource not found";

/**
 * Indicates that a resource could not be stored in the file system due to an error.
 */
export const FILE_SYSTEM_ERROR = "file system error";

/**
 * Indicates that the requested service could not be fulfilled due to an invalid argument.
 */
export const INVALID_ARGUMENT = "invalid argument";
